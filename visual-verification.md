# 視覺驗證紀錄

## BattleCalculator 第二階段

- 桌面版 `/battle`：首次截圖顯示正式題庫載入中的安全降級畫面，標題、說明與導覽對比清楚，未出現版面破裂。
- 375px `/battle`：可見戰鬥場場景、學生可理解的「準備探索」狀態卡、雙方生命值、棲息地選擇卡及開始對戰按鈕；內容採單欄堆疊，文字未被裁切，觸控按鈕保有足夠間距。
- 連擊、暴擊與守門者節奏提示已另由 BattleScene 回歸測試覆蓋，避免依賴截圖中的隨機戰鬥流程。

## 戰鬥視覺回饋第三階段

- 桌面版 `/battle`：首次顯示正式題庫載入中的安全畫面，未見新增生命值讀值或動畫容器造成的版面位移、溢位或導覽遮擋。
- 375px `/battle`：戰鬥場、雙方生命值數字讀值與生命條維持單欄可讀；棲息地選擇與開始對戰按鈕未因角色動畫容器增加而擠壓或裁切。
- 實際衝刺、命中閃光與動畫結束清理由 BattleScene 的假計時器回歸測試驗證；CSS 僅在不偏好減少動態時啟用，並保留靜態生命值狀態。

## 勝利航線與補給標記連動驗證（2026-08-19）

桌面版 `/map` 與 `/battle` 已完成檢視：導覽、主內容層級與戰鬥入口維持清楚；勝利連動的航線與補給標記由真實狀態 props 驅動，不改寫既有地圖卡片結構。375px 版已完成檢視：主航海圖入口卡片、上方導覽與戰鬥場的返回地圖入口均維持可讀，補給／航線樣式使用手機優先規則並保留 reduced-motion 降級。測試與 production build 均通過。

## 勝利寶藏揭示與地圖連動視覺驗證（2026-08-19）

桌面版 `/battle` 與 `/map` 入口的導覽層級、戰鬥容器與主航海圖卡片維持清晰；375px 版戰鬥場的角色、HP 與答題區未因結果卡預留空間而擁擠，地圖頁的補給／航線摘要可在單欄版面閱讀。此次截圖為入口與載入後初始狀態，勝利卡本身另由 BattleScene 回歸測試覆蓋；動畫樣式保留 reduced-motion 靜態降級。

## 補給標記學習入口驗證（2026-08-19）

桌面版 `/map` 已確認主航海圖的島嶼、航線與資訊卡容器在新增補給操作樣式後維持清楚分層；375px 版地圖以單欄顯示，島嶼標記、文字與主內容未被裁切。策略面板僅由真實補給標記驅動，其顯示、收合、朗讀按鈕、ARIA 展開狀態與同科練習導向，另由 `TaiwanMainNavigationMap` 回歸測試覆蓋。完整 77 個測試檔／328 項測試與 production build 已通過，策略展開動畫提供 `prefers-reduced-motion` 靜態降級。

## 航線微光策略互動驗證（2026-08-19）

桌面版 `/map` 顯示主航海圖、島嶼節點與航線層次維持清楚；可互動微光航線沿用既有金黃微光語彙，聚焦時增加柔和外框，不會遮蔽島嶼資訊卡。375px 手機版地圖以窄幅單欄呈現，航線與島嶼標記仍可辨識，沒有因互動樣式造成水平溢出；策略摘要仍由島嶼資訊卡提供，航線入口僅在真實新增補給標記的短暫微光期間出現。點擊、Enter 與 Space 導向，以及非微光航線保持不可互動，均由主航海圖回歸測試覆蓋。完整 77 個測試檔／331 項測試與 production build 通過。

## 航線微光首次使用提示驗證（2026-08-19）

桌面與 375px 主航海圖皆確認圖例區保有足夠的文字與膠囊提示空間，不會遮擋島嶼、航線或後續資訊卡。首次遇到真實補給航線時，提示以短暫的正向文案說明「微光航線可以點一下，查看補給策略」；顯示、1400ms 自動收起、點擊航線後提前收起與 localStorage 一次性紀錄由 `TaiwanMainNavigationMap` 回歸測試覆蓋。減少動態偏好下，提示保留完整可讀文字但不播放進場動畫。
## 補給策略摘要收起操作驗證（2026-08-19）

桌面版 `/map` 的策略摘要保留毛玻璃卡片層次，新增「關閉策略」與「朗讀策略」雙按鈕列不會壓縮既有文字；375px 版按鈕以可觸控尺寸排列，窄螢幕下維持清楚間距與換行。策略摘要可由明確關閉按鈕或摘要外空白處收起，Escape、焦點回復、朗讀與 `prefers-reduced-motion` 降級均由回歸測試覆蓋。完整測試 77 個檔案／333 項通過，production build 通過。

## 補給策略摘要狀態記憶驗證（2026-08-19）

桌面版 `/map` 檢視確認主航海圖入口、資訊卡與策略操作列維持原有層次；狀態記憶只影響摘要是否展開，不改變島嶼、航線或其他學習資料的視覺呈現。375px 手機版檢視確認地圖導覽、入口按鈕與卡片以單欄堆疊，沒有因偏好恢復而造成裁切或操作擁擠。狀態恢復與 localStorage 失敗降級由新增回歸測試及完整測試套件覆蓋；本次 build 亦已通過。


## 補給策略摘要依島嶼狀態記憶驗證（2026-08-19）

桌面版 `/map` 確認資訊卡、島嶼切換與策略操作列維持清楚層次；個別狀態恢復不造成額外版面位移。375px 版確認摘要操作列、島嶼切換與地圖互動空間仍可讀可按，沒有因個別偏好恢復而裁切或擁擠。依島嶼持久化、舊格式遷移、localStorage 失敗降級與切換隔離由回歸測試覆蓋；完整測試與 production build 已通過。
